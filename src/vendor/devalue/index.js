// Vendored from https://github.com/lucacasonato/devalue (fork of
// https://github.com/sveltejs/devalue). Inlined while the fork is
// unpublished so the package doesn't depend on a git URL. Only the
// `uneval` entrypoint is used — `parse`/`stringify` are not vendored.
// Licensed MIT (see ./LICENSE).
export { uneval } from "./uneval.js";
